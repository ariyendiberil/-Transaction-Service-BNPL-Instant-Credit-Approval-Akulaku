/**
 * Interfaces layer — driving adapters.
 *
 * <p>Entry points to the application: REST controllers, request/response DTOs,
 * exception handlers (RFC 7807 Problem Details), input filters
 * (idempotency, correlation id). Translates HTTP world into use case commands.
 */
package com.akulaku.transaction.interfaces;
